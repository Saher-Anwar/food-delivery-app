# Terraform state files (never commit these!)
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*.backup
*.tfstate
*.tfstate.*

# Local Terraform directories (plugins, lock files)
.terraform/
.terraform.lock.hcl

# Crash logs (if any)
crash.log